# 📱 دليل التفعيل — خطوة بخطوة

الهدف: تشغيل النظام تلقائيًا كل 4 ساعات، وفتحه من جوالك كأيقونة تطبيق.
كل الخطوات التالية **لمرة واحدة فقط**. بعدها كل شيء يعمل لوحده.

⏱️ الوقت المتوقع: 15–20 دقيقة.
💰 التكلفة: صفر (كل الخدمات المستخدمة مجانية).

---

## الخطوة 1 — أنشئ حساب GitHub مجاني

1. افتح: **github.com/signup**
2. أدخل بريدك الإلكتروني وكلمة مرور واسم مستخدم (أي اسم تختاره).
3. فعّل الحساب من رسالة التأكيد في بريدك.

> GitHub خدمة مجانية لتخزين المشاريع البرمجية. لن تحتاج معرفة برمجية — فقط رفع ملفات وضغط أزرار.

---

## الخطوة 2 — أنشئ مستودعًا (Repository) جديدًا

1. بعد تسجيل الدخول، اضغط علامة **+** أعلى اليمين ← **New repository**.
2. في خانة **Repository name** اكتب: `reverse-split-hunter`
3. اختر **Public** (مهم: GitHub Pages المجاني يحتاج المستودع أن يكون Public).

> ⚠️ **انتبه لهذه النقطة:** Public يعني أن أي شخص يملك الرابط يستطيع رؤية قائمة أسهمك وبياناتك. إذا كان هذا يزعجك، اختر **Private** بدلًا من ذلك — لكن حينها لن تعمل استضافة GitHub Pages المجانية، وستحتاج تفتح الداشبورد يدويًا من ملف تنزّله بنفسك. القرار لك.

4. اضغط **Create repository**.

---

## الخطوة 3 — ارفع ملفات المشروع

1. في صفحة المستودع الجديد، اضغط **uploading an existing file** (رابط أزرق في المنتصف).
2. اسحب **كل ملفات المشروع** التي أرسلتها لك (كل ملفات `.py` + `dashboard_shell.html` + `requirements.txt` + مجلد `.github`).
3. اسحب أيضًا **ملف Excel** الخاص بك (`ملف المتابعة.xlsx`) إذا أردت الاستيراد منه.
4. اضغط **Commit changes** أسفل الصفحة.

> 📌 إذا لم يظهر مجلد `.github` عند السحب (بعض الأنظمة تخفي المجلدات التي تبدأ بنقطة)، أنشئه يدويًا:
> اضغط **Add file ← Create new file**، واكتب في خانة الاسم بالضبط:
> `.github/workflows/auto_update.yml`
> ثم الصق محتوى الملف واضغط Commit.

---

## الخطوة 4 — فعّل التشغيل التلقائي (GitHub Actions)

1. في مستودعك، افتح تبويب **Actions** (بالأعلى).
2. إذا ظهرت رسالة تطلب تفعيل Workflows، اضغط **I understand my workflows, go ahead and enable them**.
3. اختر من القائمة اليسرى: **Reverse Split Hunter — Auto Update**.
4. اضغط **Run workflow** ← ثم **Run workflow** مرة أخرى (لتشغيله فورًا دون انتظار 4 ساعات).

انتظر 2–5 دقائق. إذا ظهرت علامة ✅ خضراء، نجح التشغيل.
إذا ظهرت ❌ حمراء، اضغط عليها وانسخ رسالة الخطأ وأرسلها لي.

---

## الخطوة 5 — فعّل الاستضافة (GitHub Pages)

1. افتح تبويب **Settings** ← من القائمة اليسرى اختر **Pages**.
2. تحت **Build and deployment** ← في خانة **Source** اختر: **GitHub Actions**.
3. احفظ. بعد دقائق سيظهر لك رابط بهذا الشكل:

```
https://اسم-المستخدم.github.io/reverse-split-hunter/
```

هذا هو رابطك الثابت الدائم. احفظه.

---

## الخطوة 6 — أضفه لشاشة جوالك (Galaxy S25 Ultra)

1. افتح **Chrome** على جوالك.
2. الصق الرابط من الخطوة 5 وافتحه.
3. اضغط زر القائمة **⋮** (أعلى اليمين).
4. اختر **إضافة إلى الشاشة الرئيسية** (Add to Home screen).
5. اضغط **إضافة**.

✅ ستظهر أيقونة باسم **RS Hunter** على شاشتك، تفتح بملء الشاشة مثل أي تطبيق عادي.

---

## كيف يعمل بعد ذلك؟

| ماذا | متى |
|---|---|
| جلب السعر / RSI / الحجم / Float | تلقائيًا كل 4 ساعات |
| جلب ملفات SEC وأحداث Reverse Split | تلقائيًا كل 4 ساعات |
| حساب الحالات والتنبيهات | تلقائيًا كل 4 ساعات |
| تحديث الداشبورد على جوالك | تلقائيًا — فقط افتح الأيقونة |
| **Borrow / CTB / Short Interest** | **يدويًا منك** (لا يوجد مصدر مجاني) |

---

## تحديث بيانات Borrow / CTB يدويًا

هذه البيانات الوحيدة التي تحتاج تدخلك (لأن مصادرها مدفوعة ~$30–100 شهريًا).

الطريقة: حدّث الأعمدة في ملف Excel الخاص بك، ثم ارفعه للمستودع مجددًا:
**Add file ← Upload files ← اسحب الملف ← Commit changes**

سيلتقطه النظام في الدورة التالية تلقائيًا.

> 💡 معلومة مهمة: إذا مرّت دورة تحديث تلقائية ولم تُدخل Borrow جديدًا، النظام **يحتفظ بآخر قيمة أدخلتها** ولا يفقدها، ويسجّل تاريخ قراءتها الأصلي حتى تعرف مدى حداثتها.

---

## إذا توقف شيء عن العمل

| المشكلة | الحل |
|---|---|
| علامة ❌ حمراء في تبويب Actions | اضغط عليها، انسخ رسالة الخطأ، أرسلها لي |
| الداشبورد لا يتحدث | تأكد أن Settings ← Pages ← Source = GitHub Actions |
| بيانات السوق فارغة | غالبًا Yahoo Finance غيّر شيئًا — أخبرني وسأصلحه |
| الرابط لا يفتح | انتظر 5 دقائق بعد أول تشغيل ناجح، ثم أعد المحاولة |

---

## ⚠️ ملاحظات صادقة مهمة

1. **لم أستطع اختبار هذه المرحلة فعليًا** — بيئة التطوير التي بُني فيها الكود بلا اتصال إنترنت، فلم أتمكن من تجربة الاتصال الحقيقي بـ Yahoo Finance أو تشغيل GitHub Actions حيًا. اختبرت المنطق الداخلي بردود وهمية (61 اختبارًا ناجحًا). **توقّع وجود أخطاء صغيرة عند أول تشغيل حقيقي** — أرسل لي أي رسالة خطأ وسأصلحها فورًا، كما فعلنا في كل المراحل السابقة.

2. **Yahoo Finance مصدر غير رسمي** — لا يوجد عقد أو ضمان من Yahoo، وقد يتغيّر أو يُحظر مستقبلًا دون إشعار. هذا احتمال وارد وسهل الإصلاح عند حدوثه.

3. **قبل تشغيل `sec_monitor.py`** — يجب تعديل هذا السطر في `config.py` ووضع بريدك الحقيقي، لأن SEC تحظر الطلبات بدون بريد تعريفي:
```python
SEC_USER_AGENT = "اسمك بريدك@example.com"
```

4. **هذا النظام أداة بحث ومراقبة، وليس نصيحة استثمارية.** الأرقام والحالات (COILED/READY/HOT) تصف تغيّر البيانات فقط ولا تتنبأ بحركة السهم. قرارات التداول مسؤوليتك وحدك، والأسهم Micro-Cap منخفضة السيولة تحمل مخاطر عالية.
