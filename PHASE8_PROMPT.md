# برومبت: أتمتة البيانات + تطبيق جوال (Reverse Split Hunter — Phase 8)

## الهدف
تحويل النظام الحالي (Phases 1-7، يعمل يدويًا عبر استيراد Excel) إلى نظام **يحدّث نفسه تلقائيًا يوميًا** ويُعرض كصفحة ويب تُضاف لشاشة جوال Samsung Galaxy S25 Ultra مثل تطبيق عادي (Add to Home Screen)، بدون أي تعقيد إضافي على المستخدم.

**لا تغيّر الاستراتيجية الأصلية ولا القواعد المبنية في Phases 1-7. هذا الـ Phase يضيف طبقة "تغذية بيانات تلقائية" فوق النظام الموجود فقط.**

---

## 1) مصادر البيانات

### أ) تلقائي بالكامل (بدون تدخل بشري، بدون اشتراك مدفوع)
- **السعر (Price/Open/High/Low/Close)، الحجم (Volume)، المتوسطات، RSI(14)** ← Yahoo Finance (عبر مكتبة `yfinance` أو طلب HTTP مباشر لـ query1.finance.yahoo.com).
- **Shares Float التقريبي** ← Yahoo Finance / السكان العام (نفس مصدر ورقة2 الحالي لكن مُحدَّث تلقائيًا).
- **أحداث Reverse Split وملفات SEC** ← نفس `sec_monitor.py` المبني في Phase 7 (SEC EDGAR)، يعمل تلقائيًا كجزء من الجدولة اليومية.

### ب) يبقى يدويًا كما هو (بدون تغيير عن النظام الحالي)
- **Borrow (الأسهم المتاحة للاقتراض)، CTB (تكلفة الاقتراض)، Short Interest.**
- السبب: لا يوجد مصدر مجاني موثوق لهذه البيانات؛ المصادر المتخصصة (Ortex, Fintel, S3 Partners) مدفوعة (~$30-100+/شهر) وتحتاج ربط API منفصل. أُبقيها يدوية حفاظًا على البساطة كما طلب المستخدم صراحة.
- طريقة الإدخال: نفس آلية "تحديث يدوي" المبنية مسبقًا في `manual_updates`.

---

## 2) الجدولة (Automation)

- **كل 4 ساعات** (تحديث متكرر خلال اليوم لمتابعة أقرب للحظة الفعلية دون مبالغة).
- التنفيذ عبر **GitHub Actions** (خدمة مجانية مرتبطة بحساب GitHub، تسمح بتشغيل سكريبت Python على جدول زمني تلقائيًا بدون أي سيرفر يملكه المستخدم).
- خطوات كل دورة تحديث (كل 4 ساعات):
  1. جلب بيانات السوق (Yahoo) لكل الشركات ضمن `NEEDS BORROW CHECK` / `HUNTING WATCHLIST`.
  2. جلب Filings جديدة من SEC (`sec_monitor.py`).
  3. تشغيل خط المعالجة كاملًا بنفس الترتيب الحالي: `reverse_split.py → float_filter.py → short_pressure.py → low_float_ignition.py → change_detector.py → alerts.py`.
  4. تصدير الداشبورد (`export_dashboard_data.py`) وتحديث ملف HTML.
  5. نشر الملف تلقائيًا عبر **GitHub Pages** (استضافة مجانية لصفحات HTML الثابتة) على رابط ثابت دائم.

---

## 3) التطبيق على الجوال

- **لا يوجد تطبيق Android حقيقي (APK)** — بدلًا من ذلك: صفحة ويب واحدة على رابط ثابت (من GitHub Pages).
- المستخدم يفتح الرابط من متصفح الجوال (Chrome) مرة واحدة، ثم يستخدم خيار **"إضافة إلى الشاشة الرئيسية" (Add to Home Screen)**.
- النتيجة: أيقونة على شاشة الجوال تفتح بملء الشاشة بدون شريط عنوان المتصفح — تجربة استخدام مطابقة لتطبيق حقيقي.
- الصفحة تُحدَّث تلقائيًا كل 4 ساعات بدون أي فعل من المستخدم؛ يكفي فتحها لرؤية آخر تحديث.

> ملاحظة اطمئنان: GitHub تمنح كل حساب مجاني ~2000 دقيقة تشغيل شهريًا لـ GitHub Actions. تحديث كل 4 ساعات = 6 مرات يوميًا (~180 مرة شهريًا)، وكل تشغيلة متوقَّع أن تستغرق دقائق قليلة فقط — هذا ضمن الحد المجاني بارتياح.

---

## 4) ما يحتاجه المستخدم فعليًا (لمرة واحدة فقط)
1. إنشاء حساب GitHub مجاني (بريد إلكتروني + كلمة مرور، مثل أي حساب عادي).
2. اتباع خطوات مبسّطة (نسخ ولصق) لرفع المشروع وتفعيل GitHub Actions + GitHub Pages — تُشرح خطوة بخطوة بالصور/الأوامر عند التنفيذ.
3. لا حاجة لأي معرفة برمجية أو تقنية إضافية بعد ذلك — النظام يعمل لوحده.

---

## 5) الملفات الجديدة المطلوب بناؤها في هذا الـ Phase
- `market_data_fetcher.py` — يجلب Price/Volume/RSI/Float من Yahoo Finance تلقائيًا ويكتبها كـ Snapshot جديد (بنفس آلية `entry_type='AUTOMATIC'` الموجودة).
- `.github/workflows/daily_update.yml` — ملف GitHub Actions يُشغّل كل الخطوات السابقة يوميًا تلقائيًا.
- تحديث بسيط على `export_dashboard_data.py` ليُنشر الناتج مباشرة داخل مجلد يخدمه GitHub Pages.
- دليل تفعيل مبسّط خطوة بخطوة (Markdown) خاص بإعداد GitHub لأول مرة.

---

## 6) قيود صادقة يجب توضيحها للمستخدم
- Yahoo Finance مصدر غير رسمي (لا API عقد رسمي)، وقد يتغيّر شكله دون إشعار مسبق — إذا توقف الجلب التلقائي مستقبلًا، هذا سبب متوقّع وسهل الإصلاح.
- البيئة الحالية لبناء هذا الـ Phase **بلا اتصال إنترنت**، فلن أستطيع اختبار الاتصال الفعلي بـ Yahoo Finance أو GitHub Actions حيًا أثناء الكتابة — سأكتب الكود بعناية وأطلب من المستخدم تجربته وإخباري بأي خطأ فور أول تشغيل حقيقي، تمامًا كما حدث مع Phases السابقة.
- Borrow/CTB يبقيان يدويين بشكل دائم ضمن هذا النطاق، ما لم يقرر المستخدم لاحقًا الاشتراك بمصدر مدفوع.
